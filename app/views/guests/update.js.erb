var el = document.getElementById('<%= @guest.dom_id %>')
el.outerHTML = '<%= j render(partial: 'welcome/guest', object: @guest) %>'
document.body.insertAdjacentHTML('afterbegin', '<%= j pf_toast_notification %>')
$('select').selectpicker('refresh');
